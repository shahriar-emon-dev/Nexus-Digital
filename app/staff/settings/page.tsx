import { RouteScaffold } from "@/components/shared/RouteScaffold";

export const metadata = { title: "Settings" };

export default function StaffSettingsPage() {
  return <RouteScaffold title="Settings" route="/staff/settings" />;
}
